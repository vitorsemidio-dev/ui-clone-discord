import styled from 'styled-components';

interface UserRowPros {
  nickname: string;
}

export const Container = styled.div``;

export const Role = styled.div``;

export const UserRow = styled.div<UserRowPros>``;
